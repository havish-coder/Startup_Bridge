import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

// Placeholder — App.jsx will be wired in Sprint 1 Step 2
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-3xl font-bold text-gray-900">StartupBridge</h1>
        <p className="mt-2 text-gray-500">Initialization complete — ready to build</p>
      </div>
    </div>
  </React.StrictMode>
)
